package com.example.sacco_core_banking.enums;

/** Category of action a WorkFlowTransition performs, shown as "Action Type" in the Transitions tab. */
public enum TransitionActionType {
    NORMAL_SUBMISSION,
    APPROVAL,
    REJECTION,
    RETURN
}
